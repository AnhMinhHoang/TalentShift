server:
  error:
    include-message: always
    include-binding-errors: always
    include-exception: true
spring:
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB
  datasource:
    username: root
    password: GoldCandy562004
    url: jdbc:mysql://localhost:3306/talentshift
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true

# File upload configuration
file:
  upload-dir: ./uploads
# Day la cho debug loi neu khong xuat hien tu front end sang
# logging:
#   level:
#     root: DEBUG
#     org.hibernate.orm.results: WARN
#     org.hibernate.orm.mapping.natural_id: WARN
